{
  "schemaVersion": 1,
  "id": "yeleticc.vpn",
  "name": "VPN",
  "version": "1.1.0",
  "author": "yeleticc",
  "license": "MIT",
  "description": "WireGuard status, tunnel health, reconnecting, and switching in the Omarchy bar, driven by wg-quick over the profiles in ~/.config/omarchy/vpn/profiles/.",
  "kinds": [
    "bar-widget"
  ],
  "entryPoints": {
    "barWidget": "Panel.qml"
  },
  "barWidget": {
    "displayName": "VPN",
    "description": "Connects and disconnects WireGuard profiles from ~/.config/omarchy/vpn/profiles/ using wg-quick, and shows the public IP while a tunnel is up.",
    "category": "Network",
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "refreshIntervalSec": 15,
      "profilesDir": "~/.config/omarchy/vpn/profiles"
    },
    "schema": [
      {
        "key": "refreshIntervalSec",
        "type": "integer",
        "label": "Refresh interval (seconds)",
        "min": 5,
        "max": 600,
        "step": 5,
        "defaultValue": 15
      },
      {
        "key": "profilesDir",
        "type": "string",
        "label": "Profiles directory",
        "defaultValue": "~/.config/omarchy/vpn/profiles",
        "description": "Directory holding *.conf WireGuard profiles. ~ is expanded to the home directory."
      }
    ]
  }
}
